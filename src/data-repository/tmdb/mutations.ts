import { MutationRequests } from 'types/ApiTypes'

const mutations: MutationRequests = {
  /*makeList: (variables) => ({
    endpoint: `movie/${variables.name}`,
    method: 'POST',
    resolver: (data) => ({
      status: false,
    }),
  }),*/
}

export default mutations
